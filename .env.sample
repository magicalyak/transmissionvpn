# .env - Docker Run Configuration
# Environment variables for the nzbgetvpn container

# --- Core VPN Settings -------------------------------------------------------
VPN_CLIENT=openvpn
# Choose your VPN client: "openvpn" or "wireguard"

VPN_CONFIG=
# Path to your VPN configuration file inside the container.
# - For OpenVPN: e.g., /config/openvpn/your_provider.ovpn
# - For WireGuard: e.g., /config/wireguard/wg0.conf
# If left empty, the script will try to use the first .ovpn (for OpenVPN) or .conf (for WireGuard)
# file found in the respective /config/openvpn or /config/wireguard directory.

# VPN_CREDENTIALS_FILE=
# Optional: Path to OpenVPN credentials file (username on line 1, password on line 2).
# If this is set and the file exists, VPN_USER and VPN_PASS below will be ignored.
# Example: /config/vpn_credentials.txt

# --- OpenVPN Specific Credentials (if not using VPN_CREDENTIALS_FILE) --------
# These are ignored if VPN_CREDENTIALS_FILE is set and valid.
VPN_USER=
# Your OpenVPN username.
VPN_PASS=
# Your OpenVPN password.

# VPN_OPTIONS=--inactive 3600 --ping-exit 60
# Optional: Additional command-line options for the OpenVPN client.

# --- Network & DNS Settings -------------------------------------------------
# NAME_SERVERS=1.1.1.1,8.8.8.8
# Optional: Comma-separated list of DNS servers to use (e.g., "1.1.1.1,8.8.8.8").
# - OpenVPN: If empty, attempts to use DNS pushed by VPN. If none, uses fallbacks.
# - WireGuard: If empty, uses DNS from WireGuard config. NAME_SERVERS here will override it.

# LAN_NETWORK=192.168.1.0/24
# Optional: Your LAN network (CIDR notation, e.g., 192.168.1.0/24).
# Allows access to/from your local network, bypassing the VPN for these connections.

# ADDITIONAL_PORTS=
# Optional: Comma-separated list of additional TCP/UDP ports to allow outbound through the VPN.
# E.g., for a script that needs to connect on port 9090: "9090" or for UDP on 53: "53/udp"

# --- Privoxy Settings -------------------------------------------------------
ENABLE_PRIVOXY=no
# Enable Privoxy HTTP proxy ("yes" or "no")

# PRIVOXY_PORT=8118
# Optional: Define the port Privoxy listens on inside the container.
# Default is 8118. If you change this, adjust host port mapping in docker run.

# --- General Container Settings (from linuxserver.io base) ------------------
PUID=1000
# User ID for NZBGet process. Match to your host user for easy file permissions.
# Find yours with `id -u` on Linux/macOS.
PGID=1000
# Group ID for NZBGet process. Match to your host user for easy file permissions.
# Find yours with `id -g` on Linux/macOS.

TZ=Etc/UTC
# Timezone for the container, e.g., America/New_York, Europe/London

UMASK=022
# File creation mask for NZBGet. 022 is a common default (user rwx, group rx, other rx).
# 002 would give group write access as well.

# --- NZBGet Specific (Optional - can be set in NZBGet UI later) ------------
# NZBGET_USER=
# NZBGET_PASS=

# --- Debugging --------------------------------------------------------------
# DEBUG=false
# Enable verbose script logging (set -x) by setting to "true"